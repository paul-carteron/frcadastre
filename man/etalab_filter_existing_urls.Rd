% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/etalab.R
\name{etalab_filter_existing_urls}
\alias{etalab_filter_existing_urls}
\title{Filter and validate existing URLs in a data.frame}
\usage{
etalab_filter_existing_urls(df, verbose = TRUE)
}
\arguments{
\item{df}{A \code{data.frame} with columns \code{insee_code}, \code{data}, and \code{url}.}

\item{verbose}{Logical (default = \code{TRUE}).
If \code{TRUE}, display warnings for removed URLs and a message about how many were kept.}
}
\value{
A filtered \code{data.frame} containing only rows with valid and accessible URLs.
}
\description{
This function checks if the URLs in a data.frame are accessible.
Invalid or unreachable URLs are removed from the result.
Optionally, warnings and messages are displayed if \code{verbose = TRUE}.
}
\examples{
# Example data
df <- data.frame(
  insee_code = c("00001", "00002"),
  data = c("example1", "example2"),
  url = c("https://www.r-project.org", "https://nonexistent.example.com"),
  stringsAsFactors = FALSE
)

# Filter valid URLs
etalab_filter_existing_urls(df, verbose = TRUE)

}
\seealso{
\link{.url_exists} for the low-level URL existence check.
}
