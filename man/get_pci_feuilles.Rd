% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pci.R
\name{get_pci_feuilles}
\alias{get_pci_feuilles}
\title{Retrieve PCI Sheet URLs for a Commune}
\usage{
get_pci_feuilles(
  commune,
  millesime = "latest",
  format = "edigeo",
  absolute = TRUE
)
}
\arguments{
\item{commune}{\code{character} vector. The INSEE code(s) of the commune(s).}

\item{millesime}{\code{character}. The version or millesime of the dataset.
Must be on of \code{get_data_millesimes("pci")}. Default is \code{"latest"}.}

\item{format}{\code{character}. The format of the data.
Must be \code{"edigeo"} or \code{"dxf"}. Default is \code{"edigeo"}.}

\item{absolute}{\code{logical}. Default is \code{"TRUE"}.
If \code{TRUE} (default), returned links are converted to absolute URLs.
If \code{FALSE}, relative paths are preserved.}
}
\value{
A character vector of URLs (or relative sheet identifiers if \code{absolute = FALSE}).
}
\description{
Returns the URLs of PCI sheets (feuilles) for a given commune.
Can return either absolute URLs or relative sheet identifiers.
}
\details{
PCI (Plan Cadastral Informatise) sheets are organized per commune. This function
constructs the URLs to access all available sheets for the requested communes.
When \code{absolute = FALSE}, the returned sheet identifiers are cleaned by removing
the format prefix (\code{edigeo} or \code{dxf}) and the \code{.tar.bz2} file extension.
}
\examples{
\dontrun{
# Retrieve absolute URLs for a single commune
get_pci_feuilles("72187")

# Retrieve relative sheet names
get_pci_feuilles("72187", absolute = FALSE)
}

}
\seealso{
\code{\link[=get_data_millesimes]{get_data_millesimes()}}
}
