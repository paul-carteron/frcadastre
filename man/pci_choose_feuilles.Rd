% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pci.R
\name{pci_choose_feuilles}
\alias{pci_choose_feuilles}
\title{Detect PCI feuille archives for a commune}
\usage{
pci_choose_feuilles(
  commune,
  format = "edigeo",
  feuille = NULL,
  skip = TRUE,
  ...
)
}
\arguments{
\item{commune}{Character. Commune code (e.g., INSEE code).}

\item{format}{Character. Data format, either \code{"edigeo"} or \code{"dxf"}. Default is \code{"edigeo"}.}

\item{skip}{Logical. If TRUE, strips the prefix (\verb{edigeo-} or \verb{dxf-}) and suffix (\code{.tar.bz2}) from the archive names.
Default is FALSE.}

\item{...}{Additional arguments passed to \code{pci_construct_url()}.}
}
\value{
Character vector of archive names (with or without prefix/suffix depending on \code{skip}).
}
\description{
Retrieve the list of available PCI (Plan Cadastral Informatisé) feuille (sheet) archive names
for a given commune and format.
}
\details{
This function constructs the URL for PCI feuille scale data for the specified commune and format,
then detects and returns the available archive links.
}
\examples{
\dontrun{
# List available Edigeo feuille archives for a commune
archives <- pci_detect_feuilles("72187")

# Get archive names without prefix and suffix
archives_clean <- pci_detect_feuilles("72187", skip = TRUE)
}

}
