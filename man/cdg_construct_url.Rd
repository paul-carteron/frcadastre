% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdg.R
\name{cdg_construct_url}
\alias{cdg_construct_url}
\title{Construct a Data Download URL}
\usage{
cdg_construct_url(
  site,
  format,
  scale,
  millesime = NULL,
  allowed_sites = c("pci", "etalab"),
  allowed_formats,
  allowed_scales
)
}
\arguments{
\item{site}{\code{character}. Site identifier. Must be one of \code{allowed_sites}.}

\item{format}{\code{character}. Data format. Must be one of \code{allowed_formats}.}

\item{scale}{\code{character}. Data scale. Must be one of \code{allowed_scales}.}

\item{millesime}{\code{character} or \code{NULL}. The desired data version.
If \code{NULL}, the most recent millesime is used.}

\item{allowed_sites}{\code{character}. Vector of allowed site identifiers.
Defaults to \code{c("pci", "etalab")}.}

\item{allowed_formats}{\code{character}. Vector of allowed formats.}

\item{allowed_scales}{\code{character}. Vector of allowed scales.}

\item{...}{Additional arguments passed to \code{\link{cdg_detect_millesimes}}.}
}
\value{
A \code{character} string representing the full download URL.
}
\description{
Builds a URL for downloading data from a specified site, format, scale,
and optionally a specific "millesime" (data version).
}
\details{
\itemize{
\item The format \code{"shp"} is not available for the \code{"communes"} scale.
\item The \code{millesime} parameter can be set explicitly, or defaults to the
latest available.
}
}
\examples{
\dontrun{
cdg_construct_url(
  site = "pci",
  format = "gpkg",
  scale = "departements",
  allowed_formats = c("gpkg", "shp"),
  allowed_scales = c("communes", "departements")
)
}

}
\seealso{
\code{\link[=cdg_detect_millesimes(), cdg_aggr_url(), cdg_get_path]{cdg_detect_millesimes(), cdg_aggr_url(), cdg_get_path()}}
}
