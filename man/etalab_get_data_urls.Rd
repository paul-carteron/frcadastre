% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/etalab.R
\name{etalab_get_data_urls}
\alias{etalab_get_data_urls}
\title{Get Etalab data URLs for multiple INSEE codes and data types}
\usage{
etalab_get_data_urls(insee_code, data = NULL, ...)
}
\arguments{
\item{insee_code}{\code{character} vector.
Vector of INSEE codes (communes or departments).}

\item{data}{\code{list} or \code{NULL}. Defaut is \code{NULL}.
If \code{NULL}, retrieves URLs for all available data types for each INSEE code.
If a list, it can be named or unnamed:
\itemize{
\item unnamed list: length must match length of \code{insee_code}, each element is a vector of data types for corresponding INSEE code,
\item named list: names correspond to INSEE codes, values are vectors of data types for those codes.
}}

\item{...}{Additional arguments passed to \code{\link{etalab_construct_url}}.}
}
\value{
A \code{data.frame} with columns:
\describe{
\item{insee_code}{INSEE code (character).}
\item{data}{Data type requested (character).}
\item{url}{Constructed URL (character).}
}
}
\description{
Constructs download URLs for Etalab data for a vector of INSEE codes and optionally specified data types.
}
\details{
This function supports flexible specification of data types to retrieve for each INSEE code.
If \code{data} is NULL, all data types available for each INSEE code's scale are returned.
The function internally validates arguments and constructs URLs using \code{etalab_get_data_url()}.
}
\examples{
\dontrun{
# All data for all given INSEE codes
etalab_get_data_urls(c("75056", "69123"))

# Specific data types for each INSEE code (unnamed list)
etalab_get_data_urls(c("75056", "69123"), data = list(c("batiments"), c("voie", "parcelles")))

# Named list of data types by INSEE code
etalab_get_data_urls(
  c("75056", "69123"),
  data = list("75056" = c("batiments"), "69123" = c("voie", "parcelles"))
)
}

}
\seealso{
\code{\link[=etalab_get_data_url(), etalab_construct_url]{etalab_get_data_url(), etalab_construct_url()}}
}
