% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{cfg_get_data}
\alias{cfg_get_data}
\title{Get data configuration for a given scale}
\usage{
cfg_get_data(
  scale = c("departements", "communes"),
  as_df = FALSE,
  config = cfg_load("cdg_structure.yaml")
)
}
\arguments{
\item{scale}{\code{character}.
One of \code{"departements"} or \code{"communes"}. Specifies the scale of data to retrieve.}

\item{as_df}{\code{logical}. Default is \code{FALSE}.
If \code{TRUE}, returns a \code{data.frame} with type and data columns; if \code{FALSE}, returns a \code{list} with \code{raw_data} and \code{proc_data}.}

\item{config}{\code{list}. Defaults to loading \code{cfg_load("cdg_structure.yaml")}.
Configuration list, as returned by \code{\link{cfg_load}}.}
}
\value{
\code{list} or \code{data.frame}. If \code{as_df = FALSE}, a list with elements \code{raw_data} and \code{proc_data}. If \code{as_df = TRUE}, a \code{data.frame} with columns \code{type} (\code{"raw"} or \code{"proc"}) and \code{data}.
}
\description{
Retrieves the configuration of raw and processed data for a given scale
(e.g., "departements" or "communes") from the site configuration YAML.
Returns either a \code{list} of raw and processed data or a \code{data.frame} summarizing them.
}
\seealso{
\code{\link[=cfg_load]{cfg_load()}}
}
